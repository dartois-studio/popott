{
  "name": "popott-proto",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Prototype UX/UI de Popott — bibliotheque de plats, menu de la semaine, liste de courses",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "icons": "node scripts/icons.mjs",
    "solo": "node scripts/solo.mjs",
    "pages": "node scripts/pages.mjs",
    "verif": "node scripts/verifier-fusion.mjs && npm run solo && node scripts/verifier.mjs && npm run pages && node scripts/verifier.mjs ../../index.html --portail"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.112.2",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "jsdom": "^25.0.1",
    "sharp": "^0.33.5",
    "vite": "^6.0.7"
  }
}
